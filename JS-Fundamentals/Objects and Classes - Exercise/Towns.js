function solve(arr) {
    let array = arr.map(Number)
    let students = array.pop();
    let [ eff1, eff2, eff3 ] = array.split('');
    let hours = 0;
    
    // while (students > 0) {
    //   hours++;

    //   if ()
    // }
    // console.log(`Time needed: ${Math.ceil(needHours)}h.`);
}
solve([ '5', '6', '4', '20' ])